# Authoritative dependency list lives in ``pyproject.toml`` under
# ``[project.dependencies]``. This file mirrors it for ``pip install -r``
# users (Docker base images, CI without uv/poetry, etc.).
#
# Keep both in sync; pyproject.toml wins on conflict.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
redis>=5.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
httpx>=0.27.0
qrcode[pil]>=7.4
python-multipart>=0.0.9
stripe>=7.0.0
geoalchemy2>=0.14.0
cryptography>=41.0.0

# i18n runtime
fluent.runtime>=0.4.0
babel>=2.14

# i18n validators — Google libphonenumber Python port (Apache 2.0).
# Same de-facto standard used by Stripe / Shopify / Twilio for E.164
# parsing & country-aware phone validation.
phonenumbers>=8.13
