### KiX Platform — English (US) catalog.
### Used as the terminal fallback for any en-* locale.

welcome-message = Welcome { $name }!
    .description = You have { $count ->
        [one] 1 message
        *[other] { $count } messages
    }
